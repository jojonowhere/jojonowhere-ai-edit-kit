---
name: jojonowhere-ai-edit-kit
description: "揪揪漫遊的AI剪輯流程：把原始素材（口播＋螢幕錄影／多鏡位）整理成可以匯入Premiere的剪輯序列，並產生繁體中文字幕。使用者丟影片進來要求剪輯、或要求上字幕時使用。"
---

# JojoNowhere AI 剪輯流程

先執行 `setup.sh` 確認環境（`video-use`、ffmpeg、字幕字體、ElevenLabs金鑰都要備妥），再往下走。

## 操作順序

1. **素材檢查**：用 `ffprobe` 確認每支素材的時長、解析度、編碼、是否存在，純讀取不上傳。如果是多鏡位同時錄製（例如口播鏡頭＋螢幕錄影），先用時長把彼此配對起來。

2. **跟使用者確認剪輯架構，不要自己猜內容**：問清楚目標場次順序、每段大概多長、成品要幾個版本。同一句話講很多次時，預設抓最後一次乾淨的take；不確定要留哪個版本，就多個候選都留著，交給使用者自己篩選，不要自己先砍掉。

3. **逐字轉寫**：呼叫 `video-use/helpers/transcribe.py`（背後是 ElevenLabs Scribe v2）。**上傳前一定要先取得使用者明確同意**——講清楚檔名、用途、可能耗用額度，等到對方明確說可以才動作。轉完用 `pack_transcripts.py` 整理成帶時間碼的可讀逐字稿。

4. **定位剪點**：讀懂逐字稿內容，對應到使用者要的架構，抓出每段的起訖時間碼。抓不準的地方（例如純畫面動作、沒有對應到明確語句的部分）要老實說「這段是用猜的，需要你自己確認畫面」，不要裝得很確定。

5. **多鏡位同步**（有多支素材同時錄製時）：詳見 [`rules/多機位同步規則.md`](rules/02-多機位同步規則.md)——重點是不能只用單一固定秒差對齊，要用本機音訊比對抓出隨時間變化的漂移量。

6. **來源如果是螢幕錄影，先轉成CFR再繼續**：這類素材通常是VFR，直接用會讓Premiere預覽卡頓，處理方式見 [`rules/Premiere-XML規則.md`](rules/03-Premiere-XML規則.md) 第10條。

7. **同時產出兩份東西**：
   - 保底版：ffmpeg直接切好的小檔案，流水號命名，不能延伸剪點但保證能用。
   - 正式版：XML，引用原始長版素材，可以自由延伸剪點。詳細寫法規則見 [`rules/Premiere-XML規則.md`](rules/03-Premiere-XML規則.md)。

8. **字幕另外對成品影片做**：完整規則見 [`rules/字幕產生規則.md`](rules/04-字幕產生規則.md)——重點是要過濾掉非主要講者的雜訊、不留標點改用空白、固定輸出繁體中文。

## 安全原則

- 上傳任何素材到 ElevenLabs 前，都要重新取得那一次的明確同意，不能因為使用者之前同意過就跳過。
- 不要把 API Key 印出來、記錄下來，或要求使用者把金鑰貼進對話——金鑰只能由使用者自己寫進環境變數或 `.env`。
- 交付XML時要老實講清楚這次的結構跟上次驗證過的檔案是否完全一致，有沒有新的、沒驗證過的改動。
