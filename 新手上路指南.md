# 揪揪漫遊｜JojoNowhere 剪接工作流 新手上路指南

給第一次接觸這套「Claude剪接台＋Remotion」工作流的人看的。技術細節不重複寫在這裡，都指向 `rules/` 資料夾裡對應的規則檔（本文每一段都有連結）。

---

## 先看這個：想要精緻效果，走「生成素材＋自己貼」這條路

**如果直接在剪接台裡調完就收工，位置、大小、實際效果終究是由AI判斷、AI貼上去的——你框的只是大概範圍，最後貼得準不準、好不好看，決定權在AI手上。**

**建議的做法**：在剪接台裡框好位置/大小/時間點（這步驟本身就很準，框框資訊會完整保留，這是你跟AI溝通用的媒介），匯出剪輯計畫丟給Claude，**請Claude用FFmpeg＋Remotion生成一個個帶透明通道、尺寸對應你框出範圍的正式素材檔案（PNG序列／ProRes4444）**，你自己拖進Premiere／Final Cut／DaVinci對位。這樣會精準很多——你自己在真正的剪輯軟體裡對位，永遠比交給AI自動貼上去可靠。

剪接台的角色是「規劃跟溝通用的畫布」，不是「最終產出的工具」；預覽視窗裡看到的懸浮發光效果只是幫你抓感覺的近似版，不是最終畫質。

---

## Claude 會一直問問題，這是正常的

丟東西給Claude（不管是原始素材、還是匯出的剪輯計畫JSON）之後，**Claude通常不會馬上動手，會先問清楚**：風格要哪一種、內容有沒有歧義、時間點要不要對齊實際講話。一次問好幾個問題也是正常的，不是卡住或不會做。詳細規則見 [`rules/09-強調字貼紙內容確認規則.md`](rules/09-強調字貼紙內容確認規則.md)。

**每個問題如果列出選項，第一個通常是建議選項**——不確定要選哪個的話，直接選第一個或跟Claude說「用你建議的」都可以，不用每題都自己重新想一遍。

先問清楚再動手，是因為做錯了要重來的成本，遠比多問幾句話高。

---

## 安裝需求

跑這套流程需要本機裝好以下東西（都是免費/開源工具，一次裝好，之後每個專案共用）：

| 用途 | 工具 | 備註 |
|---|---|---|
| 影片處理 | `ffmpeg` | 剪接、轉檔、抽幀、合成檢查都靠它 |
| 正式素材算圖 | Node.js + npm | 跑Remotion；`npm install remotion @remotion/cli react react-dom` |
| Remotion算圖引擎 | 無頭Chrome | 第一次用Remotion時執行`npx remotion browser ensure`下載，約93MB |
| 逐字轉寫（本機） | `whisper-cli`（whisper.cpp） | 免費、跑在本機、不用上傳，優先用這個 |
| 逐字轉寫（雲端，選用） | ElevenLabs Scribe v2 API | 準確度較高但要上傳音檔、可能耗用額度——**每次使用前都要重新明確同意**，不是裝一次就自動授權以後都能用 |
| 多機位時間校正 | Python3 + `librosa`／`scipy` | 只有臉部+螢幕雙機位拍攝才需要 |
| 合成檢查 | Python3 + `Pillow`(PIL) | 檢查透明通道素材乾不乾淨用的 |

沒有這些工具不影響「用剪接台規劃」這一步，只有到「生成正式素材」才會用到。`setup.sh` 會幫你檢查大部分項目。

---

## 怎麼開啟「剪接台」

`tools/cut_planner.html` 是一個自包含的靜態網頁，不需要伺服器。兩種開法都行：

1. **直接在瀏覽器打開**：雙擊這個檔案，或用瀏覽器開啟它的本機路徑，功能完全一樣。
2. **請Claude（claude.ai）用Artifact側邊欄打開**：把檔案內容貼給Claude請它渲染成Artifact，會有內嵌側邊欄的介面，方便邊聊邊調——這是這套工具原本開發、測試時用的環境，細節規則見 [`rules/06-剪接台工具開發規則.md`](rules/06-剪接台工具開發規則.md)（只有你要請Claude幫你調整工具本身的介面/功能時才需要看這份）。

---

## 「剪接台」預覽視窗導覽

- **上方預覽畫面**：載入的影片，可以播放/拖曳進度（右上角「開啟影片」載入你自己的檔案）。
- **三軌時間軸**：
  - **片段**（藍）——剪輯點，可以拖邊緣調長度、拖整塊移動、分割。片段之間可以留空隙（代表要剪掉的部分），但不能重疊。
  - **強調字**（橘）——文字類內容，打的字只是「內容參考」，不是最終字體/樣式，樣式風格要另外跟Claude講清楚要哪一種（下面辭典有列）。
  - **貼紙／圖卡**（粉）——圖片類內容，可以上傳自己的圖，也可以只打文字描述（例如「炸彈」）讓Claude去找/生成對應的圖像。
- **框框大小就是你要的大小**：拖曳強調字/貼紙的框，框多大最後生成的素材規模就多大（跟裡面打的字級/文字內容無關，那些只是內容參考）。這是整套流程最重要的溝通媒介，框畫多大、放在哪，比文字描述更準。
- **匯出剪輯計畫**：按下去會跳出一個文字框（不是下載檔案，Artifact環境的下載會被擋掉，本機開啟也統一用這個流程），把裡面的JSON全選複製，貼給Claude就可以了。

---

## 建議工作流程

1. 素材丟給Claude，走 [`SKILL.md`](SKILL.md) 裡的完整剪輯流程（轉寫→架構確認→剪點→XML/保底版）。
2. 正式版影片做出來後，打開剪接台，載入這支影片。
3. 在時間軸上框出想加強調字/貼紙的位置、大小、大概時間點——不用很精準，抓感覺就好。
4. 按「匯出剪輯計畫」，複製JSON貼給Claude，附一句「照這個生成」。
5. **Claude會先問你幾個問題**（風格要哪種、內容有沒有歧義、時間點要不要對齊實際講話）——這是正常流程，不是卡住，回答完才會開始生成。
6. Claude產出一個個透明通道的正式素材檔案，附黑底/亮底的效果截圖給你先看過。
7. 你自己把素材拖進Premiere等剪輯軟體，對照原本框的位置精準對位、微調。

---

## 視覺語言小辭典

現在已經有固定名字的效果風格，下次可以直接點名要哪一種（完整規範見 [`rules/07-懸浮光暈卡片視覺規範.md`](rules/07-懸浮光暈卡片視覺規範.md)）：

- **懸浮光暈卡片**——白框＋光暈＋淡陰影＋緩慢晃動，圖片/Logo類內容的預設風格。
- **蓋章式衝擊**——文字像蓋章一樣砸下來，帶畫面震動跟一閃而過的警示色。
- **發光粗體彈出**——文字沒有卡片，就是文字本身帶光暈彈出。
- **特效類**（例如爆炸）——沒有固定外觀比例，會撐滿整個框（不是照片那種要保比例的東西）。

---

## 已知限制（不是bug，是環境特性）

- **剪接台的下載按鈕不會真的下載檔案**——瀏覽器/Artifact沙盒會擋掉，改成跳出視窗讓你複製文字。
- **剪接台裡看到的效果只是近似版**，跟Remotion生成出來的正式素材質感不會完全一樣，這是預期中的落差，不是誰做錯了。

---

## 專案資料夾慣例（下一支影片開始前先想清楚）

**每支影片開一個新的Remotion專案資料夾**（`remotion-starter/` 只是共用元件範本，複製一份出去當起點，不要直接在這個資料夾裡累積特定影片的素材）——不同影片的框框位置/時間點不會一樣，混在同一個專案裡容易搞混哪個素材對應哪支影片。完整規則見 [`rules/08-Remotion算圖管線.md`](rules/08-Remotion算圖管線.md)。

---

延伸閱讀：完整技術規則、每一條坑的來龍去脈，見 [`SKILL.md`](SKILL.md) 跟 `rules/` 資料夾。
